apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: prometheus

dashboards:
  - name: 'Momo'
    folder: ''
    type: file
    options:
      path: /var/lib/grafana/dashboards
