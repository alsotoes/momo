# NNNN-<change-id>

> Auto-generated by `make adr-sync` from `openspec/changes/<change-id>/`. Do not hand-edit — regenerate (Rule 78).

## Status
Proposed

## Confidence
High

## Context

> **Source:** `openspec/changes/<change-id>/proposal.md` ("Why" / "Problem" /
> "Problem Statement"), falling back to the spec's "Purpose". Describes the
> operational problem, failure mode, or constraint the decision addresses.

## Decision

> **Source:** `openspec/changes/<change-id>/specs/*/spec.md` requirement
> summaries. Each `### Requirement:` becomes a bullet asserting the mandated
> behavior (SHALL statements). Decisions are immutable once Accepted; change
> means a new ADR that supersedes this one (Fowler).

## Consequences

> **Source:** the spec's "Consequences" / "Impact" / "Trade-offs" section.
> Records the downstream effects, costs, and trade-offs of the decision.

## Alternatives Considered

- **Alternative A**: description — Pros: ...; Cons: ...

> Populated from the spec's "Alternatives" / "Options" section. "None
> documented." when the spec records no alternatives.

## Implementation Status

- **Code**: Planned / Partial / Done
- **Tests**: Planned / Partial / Done
- **Docs**: Planned / Partial / Done
- **Blog post**: docs/blog/posts/<NNN>-<slug>.md

> **Source:** `openspec/changes/<change-id>/tasks.md` checkbox completion,
> grouped into Code / Tests / Docs categories (Rule 78). Blog post resolved
> from the post whose artifacts reference this spec.

## References

- Issue: #<issue-number>
- PR: #<pr-number>
- Spec: `openspec/changes/<change-id>/`
- Blog: docs/blog/posts/<NNN>-<slug>.md
- Supersedes: NNNN-<superseded-change-id>

> Issue resolved from the spec's `GitHub Issue URL`; PR and blog from the
> matched blog post artifacts when present.

---

## Lifecycle

- **Proposed** — spec authored, implementation partial or not started.
- **Accepted** — all `tasks.md` checkboxes checked (code + tests + docs done).
- **Deprecated** — superseded by a newer ADR (new ADR links `Supersedes:`).

Supersession follows Fowler: never edit an Accepted ADR; write a new ADR that
marks the old one Deprecated.