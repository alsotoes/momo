# Tasks

## 1. Add Steering Rules to config.yaml
- [ ] Append rules 44-50 to the `Project Steering Rules` section in `openspec/config.yaml`

## 2. Create GitHub Issue
- [ ] Create tracking issue with `enhancement` + `automation` labels, assigned to `alsotoes`
- [ ] Link the issue URL in `proposal.md`

## 3. Verify
- [ ] Validate YAML syntax of `openspec/config.yaml`
- [ ] Confirm rules 44-50 are present and correctly formatted
- [ ] Confirm no overlap with existing rules 1-43
